{
  "scripts": {
    "build": "node webpack.config.js",
    "release": "NODE_ENV=release node webpack.config.js",
    "watch": "npm run build; onchange 'src/**/*' 'background.js' 'manifest.json' -- npm run build"
  },
  "dependencies": {
    "onchange": "^6.0.0",
    "string-replace-loader": "^1.3.0",
    "string-replace-webpack-plugin": "^0.1.3"
  },
  "devDependencies": {
    "webpack": "^4.39.1"
  }
}
